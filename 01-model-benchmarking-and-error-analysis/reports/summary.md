# Summary

This project benchmarks five supervised learning approaches on the Forest Cover Type multiclass classification task and stores artifacts in a report-ready layout.

The expected final comparison should emphasize macro-F1, balanced accuracy, per-class behavior, runtime cost, and evidence of underfitting/overfitting from model-complexity and learning curves.
